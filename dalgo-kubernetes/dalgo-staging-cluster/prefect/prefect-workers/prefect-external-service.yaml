apiVersion: v1
kind: Service
metadata:
  name: prefect-server-external
  namespace: prefect
spec:
  type: ClusterIP
  ports:
    - port: 4200
      targetPort: 4200
      protocol: TCP
      name: http